namespace Tripder.Domain.AttractionDefinition.Enums;

// Defines the rule type
public enum RuleType
{
    Weekly,       // Runs weekly
    Seasonal,     // Runs in a season
    DateException // Represents a date exception
}